const nome = "PEDRO";
/* Propriedades ou métodos de strings:
fixed
length
slice
*/
const nomeMinusculo = nome.toLowerCase();
console.log(nome);
console.log(nomeMinusculo);

//Seleciona o elemento no body do HTML:
const btn = document.querySelector(".btn");
/* Propriedades ou métodos de elementos do DOM
addEventListener
appendChild
id
innerHtml
outerHtml
*/
console.log(btn);
